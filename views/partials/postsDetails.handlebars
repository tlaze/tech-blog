<div class="container-fluid posts">
	<div class="row">
		<div class="col-md-12">
			<div class="card">
                <h5 class="card-header bg-dark text-truncate">{{title}}</h5>
                <h6 class="card-header bg-dark text-center">Posted At: {{createdAt}}</h6>
                <div class="card-body">
                    <p class="card-text text-center">{{description}}</p>
                </div>
			</div>
		</div>
	</div>
</div>